// const names: string[] = [];

// names.push("Adams");

// console.log(names)

// // Readonly Keyword that prevents an Arrays from being changed
// const unit: readonly string[] = ["Lambert"];
// unit.push("Jack") // An error is raised showing that keyword push is not applied in readonly format.

